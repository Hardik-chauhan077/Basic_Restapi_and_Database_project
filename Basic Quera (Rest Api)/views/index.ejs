<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Quera Posts</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <h1>Quera Posts</h1>
  <a href="/posts/new">➕ Create New Post</a>

  <% for (let post of posts) { %>
    <div class="post">
      <h3 class="username">@<%= post.username %></h3>
      <p class="content"><%= post.content %></p>
      <br>
      <form method="POST" action="/posts/<%= post.id %>?_method=DELETE" class="delete-form">
        <button>Delete Post</button>
      </form>

      <a href="/posts/<%= post.id %>" class="button"><button>🔍 View Details</button></a>
      <a href="/posts/<%= post.id %>/edit" class = "button"><button>Edit</button></a>
    </div>
  <% } %>
</body>
</html>
